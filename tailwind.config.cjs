const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      fontFamily: {
        'comfortaa': ['"Comfortaa"', 'cursive'],
      },
    },
  },

  plugins: [],
};

module.exports = config;
